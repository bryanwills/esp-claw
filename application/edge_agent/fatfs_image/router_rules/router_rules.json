[
  {
    "id": "startup_run_led_strip_dim_white",
    "description": "Set LED strip to low-brightness white once the device finishes booting.",
    "enabled": false,
    "consume_on_match": false,
    "ack": "startup led strip brightness set to 10",
    "match": {
      "source_cap": "app_claw",
      "event_type": "startup",
      "event_key": "boot_completed",
      "content_type": "trigger"
    },
    "actions": [
      {
        "type": "run_script",
        "input": {
          "path": "/fatfs/skills/light_switch/scripts/led_strip_switch.lua",
          "args": {
            "enabled": true,
            "brightness": 10,
            "color": {
              "r": 255,
              "g": 255,
              "b": 255
            }
          }
        }
      }
    ]
  },
  {
    "id": "im_new_session",
    "description": "Start a fresh persistent chat session when the user sends /new.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} new session started",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text",
      "text": "/new",
      "text_match_rule": "prefix"
    },
    "actions": [
      {
        "type": "call_cap",
        "cap": "new_chat_session",
        "input": {
          "alias": "{{match.remainder}}"
        }
      },
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}"
        }
      }
    ]
  },
  {
    "id": "im_list_sessions",
    "description": "List persistent chat sessions when the user sends /list.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} session list handled",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text",
      "text": "/list",
      "text_match_rule": "prefix"
    },
    "actions": [
      {
        "type": "call_cap",
        "cap": "list_chat_sessions",
        "input": {}
      },
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}"
        }
      }
    ]
  },
  {
    "id": "im_switch_session",
    "description": "Switch persistent chat sessions when the user sends /switch.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} session switch handled",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text",
      "text": "/switch",
      "text_match_rule": "prefix"
    },
    "actions": [
      {
        "type": "call_cap",
        "cap": "switch_chat_session",
        "input": {
          "alias": "{{match.remainder}}"
        }
      },
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}"
        }
      }
    ]
  },
  {
    "id": "im_delete_session",
    "description": "Delete a non-current persistent chat session when the user sends /delete.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} session delete handled",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text",
      "text": "/delete",
      "text_match_rule": "prefix"
    },
    "actions": [
      {
        "type": "call_cap",
        "cap": "delete_chat_session",
        "input": {
          "alias": "{{match.remainder}}"
        }
      },
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}"
        }
      }
    ]
  },
  {
    "id": "im_any_message_working_reply",
    "description": "Reply to any IM text message before other automation handling.",
    "enabled": true,
    "consume_on_match": false,
    "ack": "{{event.source_channel}} working reply sent",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text"
    },
    "actions": [
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}",
          "message": "🦞 ESP-Claw is snapping on it..."
        }
      }
    ]
  },
  {
    "id": "im_attachment_saved_reply",
    "description": "Reply when an IM attachment is saved.",
    "enabled": true,
    "ack": "{{event.channel}} attachment saved",
    "match": {
      "event_type": "attachment_saved"
    },
    "actions": [
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}",
          "message": "File received from {{event.source_channel}}"
        }
      }
    ]
  },
  {
    "id": "im_any_message_agent",
    "description": "Route IM text messages to the agent.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} routed to agent",
    "match": {
      "event_type": "message",
      "event_key": "text",
      "content_type": "text"
    },
    "actions": [
      {
        "type": "run_agent",
        "input": {
          "target_channel": "{{event.source_channel}}",
          "session_policy": "chat"
        }
      }
    ]
  },
  {
    "id": "agent_stage_im_notify",
    "description": "Deliver agent stage progress messages to the original IM chat.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} agent stage sent",
    "match": {
      "source_cap": "claw_core",
      "event_type": "agent_stage",
      "content_type": "text"
    },
    "actions": [
      {
        "type": "send_message",
        "input": {
          "event_type": "{{event.event_type}}",
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}",
          "message": "{{event.text}}"
        }
      }
    ]
  },
  {
    "id": "agent_out_message_send_message",
    "description": "Deliver agent output messages to the original IM chat.",
    "enabled": true,
    "consume_on_match": true,
    "ack": "{{event.source_channel}} agent output sent",
    "match": {
      "source_cap": "claw_core",
      "event_type": "out_message",
      "content_type": "text"
    },
    "actions": [
      {
        "type": "send_message",
        "input": {
          "channel": "{{event.source_channel}}",
          "chat_id": "{{event.chat_id}}",
          "message": "{{event.text}}"
        }
      }
    ]
  }
]
